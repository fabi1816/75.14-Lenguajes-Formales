(ns tlc-lisp.main-test
  (:require [clojure.test :refer :all]
            [tlc-lisp.main :refer :all]))


(deftest test-que-falla
  (testing "FIXME, I fail."
    (is (= 0 1))))

(deftest test-que-pasa
  (testing "Soy un genio!"
    (is (= 1 1))))
